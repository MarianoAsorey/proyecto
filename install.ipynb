# ============================================================
#   Instalación de paquetes necesarios para el proyecto
# ============================================================

packages <- c(
  "tidyverse",
  "ggcorrplot",
  "maps",
  "factoextra",
  "ggrepel",
  "janitor",
  "treemapify",
  "scales"
)

# Instala solo los que no estén ya instalados:
install.packages(setdiff(packages, rownames(installed.packages())))
